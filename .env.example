# Base de données (Supabase PostgreSQL)
# Récupérer depuis : Supabase → Settings → Database → Connection String
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Authentification (F-003)
# Générer avec : openssl rand -base64 32
NEXTAUTH_SECRET=""
NEXTAUTH_URL="http://localhost:3000"

# Email transactionnel — Resend (https://resend.com) ou Brevo
EMAIL_FROM="APATS ENSMG <noreply@ton-domaine.com>"
EMAIL_API_KEY=""
# EMAIL_SERVER="smtp://user:pass@smtp.example.com:587"  # alternative SMTP
